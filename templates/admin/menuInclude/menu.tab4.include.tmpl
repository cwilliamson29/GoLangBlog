{{define "MenuTab4"}}
    <div class="tab-pane fade {{if eq .MA "menuAddCat"}}show active{{end}}" id="nav-add-cat"
         role="tabpanel"
         aria-labelledby="nav-add-cat-tab">add category
    </div>
{{end}}